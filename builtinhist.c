#include "shell.h"

/**
 *builtin_hist - This function displays all the files that a user visited.
 * @run:Structure Pointer
 *  Return: 0 if (success)
 */
int builtin_hist(phoe_x *run)
{
     print_name(run->hist_toll);
    return (0);
}

